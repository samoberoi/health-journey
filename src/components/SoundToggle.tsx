interface SoundToggleProps {
  inline?: boolean;
}

// Sound toggle hidden per design — component intentionally renders nothing.
export default function SoundToggle(_: SoundToggleProps = {}) {
  return null;
}

